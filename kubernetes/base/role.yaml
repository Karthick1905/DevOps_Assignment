apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: tenant-secret-readonly
  namespace: acme-corp
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["acme-corp-db-credentials"]
    verbs: ["get"]
